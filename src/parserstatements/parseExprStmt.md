# `parseExprStmt` Function

## Overview

The `parseExprStmt` function is a crucial component of the Quantum Language compiler's parser module, responsible for parsing expressions that may be followed by semicolons or newlines. This function constructs an Abstract Syntax Tree (AST) node representing an expression statement.

### Why It Works This Way

The function handles both single and multiple expression statements separated by commas. If the next token is a comma, it treats the expression as part of a larger comma-separated expression list. This allows for complex expressions like "a += 3, b = a" to be parsed correctly, executing each expression in sequence.

## Parameters/Return Value

- **Parameters**:
  - None explicitly listed in the code snippet provided.

- **Return Value**:
  - Returns a unique pointer to an `ASTNode` containing either a single `ExprStmt` or a `BlockStmt` if multiple expressions are present.

## Edge Cases

1. **Single Expression**: When only one expression is present, the function returns a single `ExprStmt`.
2. **Multiple Expressions Separated by Commas**: The function parses multiple expressions separated by commas and groups them into a `BlockStmt`. Each expression is treated as a separate statement within the block.
3. **Semicolons and Newlines**: The function consumes any trailing semicolons or newlines after the expression(s).

## Interactions With Other Components

- **Tokenizer**: The function uses tokens returned by the tokenizer to determine the structure of the expression.
- **Error Handling**: While not shown in the snippet, the function likely interacts with error handling mechanisms to report syntax errors appropriately.
- **Parsing Context**: The function operates within the broader context of the parser, which manages the overall state and flow of parsing.

This comprehensive approach ensures that the parser can handle various forms of expression statements effectively, maintaining the integrity and correctness of the AST generated by the compiler.