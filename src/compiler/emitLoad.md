# `emitLoad` Function

## Purpose
The `emitLoad` function is designed to load a variable's value onto the stack during compilation in the Quantum Language compiler. It determines whether the variable is a local, upvalue, or global and emits the appropriate bytecode operation to load its value.

## Parameters
- **name**: A string representing the name of the variable to be loaded.
- **line**: An integer representing the current source code line number, used for error reporting and debugging purposes.

## Return Value
This function does not explicitly return a value; instead, it modifies the bytecode stream generated by the compiler through calls to `emit`.

## How It Works
1. **Resolve Local Variable**:
   - The function first attempts to find the index of the local variable named `name` within the current scope using the `resolveLocal` method.
   - If a local variable is found (`local != -1`), it emits an `Op::LOAD_LOCAL` bytecode instruction followed by the local variable index and the current line number.
   - This operation loads the value of the local variable onto the stack.

2. **Resolve Upvalue**:
   - If the local variable is not found, the function then tries to find the upvalue (a variable that is accessible from an enclosing function) named `name` using the `resolveUpvalue` method.
   - If an upvalue is found (`uv != -1`), it emits an `Op::LOAD_UPVALUE` bytecode instruction followed by the upvalue index and the current line number.
   - This operation loads the value of the upvalue onto the stack.

3. **Resolve Global Variable**:
   - If neither a local nor an upvalue is found, the function assumes that `name` refers to a global variable.
   - It adds the string representation of `name` to the string table using `addStr(name)` and resolves its index.
   - It then emits an `Op::LOAD_GLOBAL` bytecode instruction followed by the global variable index and the current line number.
   - This operation loads the value of the global variable onto the stack.

## Edge Cases
- **Undefined Variable**: If the variable `name` is not defined as either a local, upvalue, or global, the behavior is undefined. In practice, the compiler should generate an error message indicating that the variable is not declared.
- **String Table Overflow**: The `addStr` function may fail if the string table overflows, leading to potential runtime errors or crashes.

## Interactions with Other Components
- **Symbol Table**: The `resolveLocal` and `resolveUpvalue` methods rely on the symbol table to determine the location of variables. The symbol table tracks all variables defined in the current and enclosing scopes.
- **Bytecode Emission**: The `emit` function is called to append bytecode instructions to the compiled program. It takes the opcode and operands as arguments and appends them to the bytecode stream.
- **Error Handling**: While not shown in the snippet, the `emitLoad` function likely interacts with an error handling mechanism to report issues such as undefined variables or string table overflow.

In summary, the `emitLoad` function plays a crucial role in loading variable values onto the stack during compilation. By resolving the variable's scope and emitting the correct bytecode instruction, it ensures efficient execution of the compiled program. Proper interaction with the symbol table and bytecode emission mechanisms is essential for its functionality.